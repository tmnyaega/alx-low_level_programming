#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 *  * main - Entry point
 *   *
 *    * Return: Always 0 (Success)
 */
int main(void)
{
	char low='a';
	char cap='A';
		while(low <= 'z'){
		putchar(low);
		}
		while(cap <= 'Z'){
			putchar(low);  

		}
		return 0;
}
